{
  "name": "rpg",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "start": "tsc && concurrently \"tsc -w\" \"nodemon build/app.js\"",
    "runOnce": "tsc && node build/app.js",
    "test": "jest",
    "test:coverage": "jest --coverage",
    "build": "rm -rf /build && tsc"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chalk": "^4.1.1",
    "concurrently": "^6.2.0",
    "nodemon": "^2.0.7",
    "prettier": "^2.3.1",
    "prompt-sync": "^4.2.0",
    "typescript": "^4.3.2"
  },
  "devDependencies": {
    "@types/jest": "^26.0.23",
    "@types/prompt-sync": "^4.1.0",
    "jest": "^27.0.5",
    "ts-jest": "^27.0.3"
  }
}
